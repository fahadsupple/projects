# Client instructions embedded in the Meta File

Pulled verbatim out of the `Keywords` cells during intake so they do not corrupt the
parsed keyword. Source: `Meta-file.xlsx`, sheet `Meta Data (for Hardy)`.

## https://skyflex.au
- **Keyword:** pergolas melbourne
- **Instruction:** Note:For this keyword "pergolas melbourne', we just need two paragraphs of content for this keyword as the existing page has content we don't want to change. This would be the primary keyword on the page.

## https://skyflex.au/louvred-pergolas-sydney/
- **Keyword:** pergolas sydney
- **Instruction:** Note:For this keyword "pergolas sydney', we just need two paragraphs of content for this keyword as the existing page has content we don't want to change. This would be the primary keyword on the page.
