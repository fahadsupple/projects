[wsl2]
# RAM ALLOCATION - 8GB out of 16GB, leaves half for Windows
memory=8GB

# CPU ALLOCATION - 6 logical cores out of 12, leaves half for Windows
processors=6

# SWAP SPACE
swap=4GB

[experimental]
# Gradually reclaim memory back to Windows when WSL is idle
autoMemoryReclaim=gradual
